
openapi: 3.0.0
      info:
        title: Order API
        description: Manages Orders.
        version: 1.0.0
      servers:
          - url: http://localhost:8080
      paths:
          /orders:
              post:
                  summary: Create Order
                  description: Creates an order.
                  requestBody:
                      required: true
                      content:
                          application/json:
                              schema:
                                  $ref: '#/components/schemas/Order'
                  responses:
                      '201':
                          description: Order Created
                          content:
                              application/json:
                                  schema:
                                      $ref: '#/components/schemas/Order'
          /orders/{orderId}/returns:
              get:
                  summary: Get Return Details
                  description: Gets the details for a return on an order.
                  parameters:
                      - in: path
                        name: orderId
                        required: true
                        schema:
                            type: integer
                  responses:
                      '200':
                          description: Return Details
                          content:
                              application/json:
                                  schema:
                                      $ref: '#/components/schemas/Return'
              put:
                  summary: Update Return Status
                  description: Updates the status of a return on an order.
                  parameters:
                      - in: path
                        name: orderId
                        required: true
                        schema:
                            type: integer
                  requestBody:
                      required: true
                      content:
                          application/json:
                              schema:
                                  $ref: '#/components/schemas/ReturnUpdate'
                  responses:
                      '200':
                          description: Return Status Updated
                          content:
                              application/json:
                                  schema:
                                      $ref: '#/components/schemas/Return'
      components:
          schemas:
              Order:
                  type: object
                  properties:
                      order_id:
                          type: integer
                          description: The order id.
                      customer_id:
                          type: integer
                          description: The customer id.
                      order_date:
                          type: string
                          format: date-time
                          description: The order date.
                      order_total:
                          type: number
                          format: double
                          description: The order total.
                      order_status:
                          type: string
                          description: The order status.
              Return:
                  type: object
                  properties:
                      return_id:
                          type: integer
                          description: The return id.
                      order_id:
                          type: integer
                          description: The order id.
                      return_reason:
                          type: string
                          description: The reason for the return.
                      return_status:
                          type: string
                          description: The status of the return.
              ReturnUpdate:
                  type: object
                  properties:
                      return_status:
                          type: string
                          description: The updated status of the return.
